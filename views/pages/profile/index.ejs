<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/tailwind.css">
    <title>BlogNest - Profile</title>
</head>

<body class="bg-white dark:bg-gray-900">
    <%- include('../../partials/navbar') %>
        <div class="container mx-auto p-4">
            <div class="max-w-sm mx-auto bg-white dark:bg-gray-800 rounded-lg shadow-md overflow-hidden">
                <div class="flex flex-col sm:flex-row items-center p-4">
                    <img class="w-16 h-16 rounded-full" src="<%= user.photo %>" alt="Profile Picture">
                    <div class="mt-4 sm:mt-0 sm:ml-4 text-center sm:text-left">
                        <h2 class="text-xl font-semibold text-gray-900 dark:text-white">
                            <%= user.displayName %>
                        </h2>
                        <p class="text-gray-600 dark:text-gray-400">
                            <%= user.email %>
                        </p>
                        <p class="text-gray-600 dark:text-gray-400">Type: <%= user.type %>
                        </p>
                    </div>
                </div>
                <div class="p-4">
                    <a href="/auth/logout"
                        class="block w-full text-center bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded">
                        Logout
                    </a>
                </div>
            </div>
        </div>
</body>

</html>